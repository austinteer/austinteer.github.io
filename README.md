# austinteer.github.io

Personal website.

Styles are maintained directly in `assets/css/style.css`. The previous Sass/Inuit/Bower style pipeline has been removed because the site only needs a small committed stylesheet.
