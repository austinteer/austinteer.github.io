# austinteer.github.io
Personal Website

This site is published as a small static Jekyll site. The CSS and JavaScript
served from `assets/css` and `assets/js` are committed directly so GitHub Pages
can render the site without the retired Bower/Gulp asset pipeline.
